--- src/gui/CMakeLists.txt.orig	2026-03-23 11:30:46 UTC
+++ src/gui/CMakeLists.txt
@@ -100,3 +100,9 @@ endif()
         UBKeyboardPalette_linux.cpp
     )
 endif()
+
+if(CMAKE_SYSTEM_NAME STREQUAL FreeBSD)
+    target_sources(openboard PRIVATE
+        UBKeyboardPalette_freebsd.cpp
+    )
+endif()
